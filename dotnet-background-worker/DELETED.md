# Removed dotnet-background-worker

The `dotnet-background-worker` directory has been permanently removed from the repository in favor of the new `RESTAPI_Inf_Worker` project. If you need the old files, retrieve them from the commit history before this change.
